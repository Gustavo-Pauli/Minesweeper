import pygame
import scripts.settings as settings

# cell_cover_surface = pygame.image.load(settings.CELL_COVER_PATH)
# cell_cover_surface = pygame.transform.smoothscale(cell_cover_surface, (int(cell_cover_surface.get_width() * 0.25), int(cell_cover_surface.get_height() * 0.25)))
